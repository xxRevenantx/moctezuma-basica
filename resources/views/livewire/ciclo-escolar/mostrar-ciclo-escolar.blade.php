<div x-data class="space-y-4">
    <section class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm dark:border-neutral-800 dark:bg-neutral-900"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-xl font-black">Ciclos escolares registrados</h2><p class="text-sm text-slate-500">No contienen alumnos; únicamente organizan actividades académicas del periodo.</p></div><flux:input wire:model.live.debounce.350ms="search" placeholder="Buscar 2025-2026" /></div>@error('eliminar')<p class="mt-3 text-sm font-bold text-rose-600">{{ $message }}</p>@enderror</section>
    <section class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm dark:border-neutral-800 dark:bg-neutral-900"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-900 text-white"><tr><th class="p-3 text-left">Ciclo</th><th class="p-3 text-left">Estado</th><th class="p-3 text-left">Uso académico</th><th class="p-3 text-right">Acciones</th></tr></thead><tbody class="divide-y dark:divide-neutral-800">@forelse($ciclos as $ciclo)<tr><td class="p-4"><b class="text-lg">{{ $ciclo->nombre }}</b></td><td class="p-4">{{ $ciclo->es_actual ? 'Actual' : ($ciclo->cerrado_at ? 'Cerrado' : 'Histórico abierto') }}</td><td class="p-4 text-xs text-slate-500">{{ $ciclo->periodos_count }} periodos · {{ $ciclo->asignacion_materias_count }} cargas · {{ $ciclo->calificaciones_count }} calificaciones</td><td class="p-4"><div class="flex justify-end gap-2">@unless($ciclo->es_actual)<flux:button size="sm" wire:click="marcarActual({{ $ciclo->id }})">Hacer actual</flux:button>@endunless<flux:button size="sm" variant="ghost" wire:click="alternarCierre({{ $ciclo->id }})">{{ $ciclo->cerrado_at ? 'Reabrir' : 'Cerrar' }}</flux:button><flux:button size="sm" variant="danger" wire:click="eliminar({{ $ciclo->id }})">Eliminar</flux:button></div></td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-slate-500">Sin ciclos registrados.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $ciclos->links(data: ['scrollTo' => false]) }}</div></section>
    <livewire:ciclo-escolar.editar-ciclo-escolar />
</div>
